name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    name: Build ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libx11-dev \
            libxrandr-dev \
            libudev-dev \
            libgl1-mesa-dev \
            libglu1-mesa-dev \
            libasound2-dev \
            libflac-dev \
            libogg-dev \
            libvorbis-dev \
            libopenal-dev \
            libxcursor-dev \
            libxi-dev \
            libfreetype6-dev \
            libwayland-dev \
            libwayland-egl-backend-dev \
            libxkbcommon-dev \
            wayland-protocols \
            pkg-config

      - name: Configure CMake
        run: |
          if [ "${{ runner.os }}" == "Linux" ]; then
            cmake -B build -DCMAKE_BUILD_VERSION=${{ github.ref_name }} -DCMAKE_BUILD_TYPE=Release -DSFML_USE_WAYLAND=ON
          else
            cmake -B build -DCMAKE_BUILD_VERSION=${{ github.ref_name }} -DCMAKE_BUILD_TYPE=Release
          fi

      - name: Build
        run: |
          cmake --build build --config Release --parallel 4

      - name: Prepare Package (Linux/macOS)
        if: runner.os != 'Windows'
        run: |
          mkdir -p release/assets
          if [ -f "build/bin/SkyRage" ]; then
            cp build/bin/SkyRage release/
          else
            # Fallback for different output structures
            find build -name SkyRage -type f -maxdepth 4 -exec cp {} release/ \;
          fi
          chmod +x release/SkyRage
          cp -r assets/* release/assets/
          cd release
          tar -czf ../SkyRage-${{ runner.os }}-${{ github.ref_name }}.tar.gz .

      - name: Prepare Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path release/assets
          if (Test-Path "build/bin/Release/SkyRage.exe") {
            Copy-Item -Path build/bin/Release/SkyRage.exe -Destination release/
          } elseif (Test-Path "build/bin/SkyRage.exe") {
            Copy-Item -Path build/bin/SkyRage.exe -Destination release/
          } else {
            Get-ChildItem -Path build -Filter SkyRage.exe -Recurse | Select-Object -First 1 | Copy-Item -Destination release/
          }
          Copy-Item -Path assets/* -Destination release/assets/ -Recurse
          Compress-Archive -Path release/* -DestinationPath SkyRage-Windows-${{ github.ref_name }}.zip

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SkyRage-${{ matrix.os }}
          path: |
            *.tar.gz
            *.zip

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    steps:
      - name: Download Artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/*.tar.gz
            artifacts/*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
